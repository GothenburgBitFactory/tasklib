language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
before_install:
  - sudo apt-get update -qq
install:
  - "pip install -e ."
before_script:
  - sudo apt-get install -qq task
  - task --version
script: "python setup.py test"
